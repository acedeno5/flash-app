package edu.vassar.cmpu203.flash.view;

import androidx.annotation.NonNull;
import edu.vassar.cmpu203.flash.model.User;

public interface IAddTripsView {
    interface Listener {
        //void onStartTrip(final double latitude, final double longitude, @NonNull final IAddTripsView view);
        //void onEndTrip(final double latitude, final double longitude, @NonNull final IAddTripsView view);
    }

    //void updateTripDisplay(@NonNull final User user);
}
